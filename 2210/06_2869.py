A, B, V = map(int,input().split())
answer = (V-B-1)/(A-B) + 1
print(int(answer))

# 시간 복잡도에 관련된 문제이다
# 이 해답은 O(1) 이다.
# 달팽이는 하루에 (A-B) 미터 씩 올라가면 된다. 정상에 다다랐을 때는 내려올 필요가 없으므로 총 (V-B)미터 움직이면 된다.
# V-B 가 A-B로 나누어 떨어지지 않을때 1일이 추가되므로 (V-B-1)/(A-B) + 1 이라는 방정식을 짤 수 있다.
